---
language: python
python:
  - 3.5

before_install:
  - echo "Getting docs anonymously"
  - git clone https://github.com/mhassan102/quilt-docs.git
  - cd quilt-docs
  - git checkout monitor-logs-packages #remove this after merge

install:
  - echo "Installing requirements"
  - pip install -U pip
  - pip install -r monitor/requirements.txt

script:
  - echo "Execute monitoring"
  - cd monitor && python3 monitor_logs.py

notifications:
  email: false
